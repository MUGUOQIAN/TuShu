# 通用自定义字段模板
CUSTOM_TEMPLATE = """
你是一个OCR信息抽取专家。请分析上传的图片，提取以下字段：{fields}。

要求：
1. 以JSON格式输出，key为字段名，value为提取值。
2. 若未找到某字段，value设为空字符串""。
3. 对于地址、邮箱等，保持原始格式。
4. 对于金额，仅保留数字和小数点。
5. 不要输出任何JSON以外的解释文字。

输出格式：
{{
  "字段1": "值1",
  "字段2": "值2"
}}
"""

# 名片模板
BUSINESS_CARD_TEMPLATE = """
你是一位专业的商务助理，请精准提取这张名片信息。

字段列表：姓名, 公司, 职位, 手机, 座机, 邮箱, 地址

特殊规则：
- 仅输出JSON，不要任何解释。
- "地址"指公司完整地址。
- 如有多号码，"手机"优先提取标有"M"或"手机"的号码。

输出格式：
{{
  "姓名": "...",
  "公司": "...",
  "职位": "...",
  "手机": "...",
  "座机": "...",
  "邮箱": "...",
  "地址": "..."
}}
"""

# 发票模板
INVOICE_TEMPLATE = """
你是一个企业财务报销审核员，请提取这张增值税发票信息。

字段列表：发票号码, 开票日期, 购买方名称, 销售方名称, 价税合计金额, 税额

特殊规则：
- "价税合计金额"是大写金额对应的数字，不含"¥"符号。
- "开票日期"格式为YYYY-MM-DD。
- 如信息不全则留空。

输出格式：
{{
  "发票号码": "...",
  "开票日期": "...",
  "购买方名称": "...",
  "销售方名称": "...",
  "价税合计金额": "...",
  "税额": "..."
}}
"""

# 模板映射表
TEMPLATE_MAP = {
    "business_card": {
        "template": BUSINESS_CARD_TEMPLATE,
        "fields": ["姓名", "公司", "职位", "手机", "座机", "邮箱", "地址"]
    },
    "invoice": {
        "template": INVOICE_TEMPLATE,
        "fields": ["发票号码", "开票日期", "购买方名称", "销售方名称", "价税合计金额", "税额"]
    },
    "custom": {
        "template": CUSTOM_TEMPLATE,
        "fields": []  # 动态传入
    }
}